default_platform(:ios)

platform :ios do
  desc "Description of what the lane does"
  lane :custom_lane do
    # add actions here: https://docs.fastlane.tools/actions
  end

  lane :test do
  scan(
    scheme: "gitlab_actions_testTests", 
    clean: false,               
    devices: ["iPhone 15"], 
    fail_build: true,
    skip_build: true,
    xcargs: "CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO -destination 'platform=iOS'"
  )
  end
end

