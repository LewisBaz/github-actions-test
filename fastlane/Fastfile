default_platform(:ios)

platform :ios do
  desc "Run tests"
  lane :test do
    scan(
      scheme: "gitlab_actions_testTests", 
    	clean: true,               
    	devices: ["iPhone 15"], 
    	fail_build: true 
    )
  end
end

